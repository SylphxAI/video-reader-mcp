name: Publish brand alias

# RETIRED (brand-sole hard cut): @sylphx/cue is the sole public MCP package id.
# Dual-publish from @sylphx/video-reader-mcp is no longer valid.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type RETIRED (workflow intentionally fails closed)'
        required: true
        type: string

permissions:
  contents: read

jobs:
  retired:
    runs-on: sylphx-linux-standard
    steps:
      - name: Brand-sole — dual alias path retired
        run: |
          set -euo pipefail
          echo "::error::publish-brand-alias is retired. Canonical package is @sylphx/cue."
          echo "Transitional @sylphx/video-reader-mcp is not a publish source."
          exit 1
